/* autogenerated by Processing revision 1293 on 2023-08-30 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class objetos extends PApplet {

PImage img;

public void setup() {
  /* size commented out by preprocessor */;
  img = loadImage("imagen.jpg"); 
  img.resize(width, height);
  image(img, 0, 0);
  thresholdImage();
}

public void thresholdImage() {
  img.loadPixels();
  for (int i = 0; i < img.pixels.length; i++) {
    int pixelColor = img.pixels[i];
    float brightnessValue = brightness(pixelColor);
    if (brightnessValue > 150) {  // umbral
      img.pixels[i] = color(128);  // Color 
    } else {
      img.pixels[i] = color(0);    // Color negro para el fondo
    }
  }
  img.updatePixels();
  image(img, 0, 0);
}


  public void settings() { size(800, 600); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "--full-screen", "--bgcolor=#666666", "--stop-color=#cccccc", "objetos" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
